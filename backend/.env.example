# Backend Environment Variables for Render
# Add these in Render Dashboard -> Environment

# JWT Secret (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-here

# Database - Render PostgreSQL (automatically set by Render when you add a PostgreSQL database)
# DATABASE_URL=postgresql://user:password@host:5432/database

# LLM API Keys (add the ones you use)
GEMINI_API_KEY=your-gemini-api-key
NVIDIA_API_KEY=your-nvidia-api-key
MISTRAL_API_KEY=your-mistral-api-key
OPENROUTER_API_KEY=your-openrouter-api-key

# Admin Creation Secret Key (for remote admin creation)
ADMIN_SECRET_KEY=your-admin-creation-secret-key

# Port (Render sets this automatically)
PORT=3000

# Node Environment
NODE_ENV=production
